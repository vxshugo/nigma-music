import React from "react";
import {Text, View} from 'react-native'

const Player = ({
                     params,
                 }) => (
    <View>
        <Text>Player</Text>
    </View>
)

export default Player;