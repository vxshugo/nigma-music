import React from "react";
import {Text, View} from 'react-native'

const Library = ({
                        params,
                    }) => (
    <View>
        <Text>Library</Text>
    </View>
)

export default Library;